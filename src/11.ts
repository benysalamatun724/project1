// A simple TypeScript function that takes a number and returns its square root
function sqrt(x: number): number {
  return Math.sqrt(x);
}

// Example usage of the above function
console.log(sqrt(25)); // Output: 5
